"use strict";

var mongoose = require('mongoose');

var Schema = mongoose.Schema({

    id: String,
    name: String,
    thumbnail: String,
    mapTypes: String,
    mapTypesName: String,

}, {
    timestamps: true
});

module.exports = mongoose.model("maps", Schema);