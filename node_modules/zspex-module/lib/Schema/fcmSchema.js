"use strict";

var mongoose = require('mongoose');

var Schema = mongoose.Schema({
    token: String,
    id: String,
}, {
    timestamps: true
});

module.exports = mongoose.model("fcm", Schema);